spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springapp_test
    username: springuser
    password: springpass

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    org.springframework: INFO
    com.example: DEBUG
